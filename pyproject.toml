[project]
name = "neural-networks-classification"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "lightning>=2.6.1",
    "matplotlib>=3.10.8",
    "torch>=2.10.0",
    "torchvision>=0.25.0",
]

[tool.setuptools]
packages = ["neural_cv"]
